[project]
name = "<project-name>"
version = "0.1.0"
description = "<project-description>"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.3.5",
    "ruff>=0.9.9",
]
